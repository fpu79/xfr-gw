# xfr-gw

Gateway-Komponente für XFR. Das Gateway holt freigegebene Transfers vom `xfr-core` ab und stellt sie lokal per Webserver bereit.

Die Implementierung erfolgt schrittweise und bewusst schlank.
